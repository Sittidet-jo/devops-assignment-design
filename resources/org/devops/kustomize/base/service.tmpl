apiVersion: v1
kind: Service
metadata:
  name: {{SVC_NAME}}
spec:
  type: NodePort
  selector:
    app: {{APP_NAME}}
  ports:
    - name: http
      protocol: TCP
      port: {{CON_PORT}}
      targetPort: {{TGT_PORT}}
      {{NODE_PORT_BLOCK}}
