apiVersion: v1
kind: Service
metadata:
  name: test-be-api-service-canary
spec:
  type: NodePort
  selector:
    app: test-be-api
  ports:
    - name: http
      protocol: TCP
      port: 10104
      targetPort: 10104
      nodePort: 30121
