apiVersion: v1
kind: Service
metadata:
  name: test-fe-service-canary
spec:
  type: NodePort
  selector:
    app: test-fe
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30105
